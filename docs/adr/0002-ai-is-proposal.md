# ADR 0002: AI output is a proposal, never truth

Status: accepted

AI output is persisted as typed extraction/candidate data with evidence,
confidence and provider metadata. Core review/activation workflows remain the
only path to canonical knowledge. This prevents prompt injection or ambiguous
normative interpretation from silently changing education policy.

